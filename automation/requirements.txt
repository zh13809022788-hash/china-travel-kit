# chinatripbox.com autopublish pipeline dependencies
# Python 3.11+ required (uses stdlib tomllib). Tested on 3.14.

anthropic>=0.40.0        # Claude API client (content generation)
requests>=2.31.0         # Baidu / IndexNow HTTP push
python-slugify>=8.0.0    # kebab-case slug generation from titles

# tomllib is stdlib on Python 3.11+. On 3.10 or older, uncomment:
# tomli>=2.0.0

# git is invoked via subprocess (system `git` binary), no python git lib needed.
