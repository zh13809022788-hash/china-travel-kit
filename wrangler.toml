# Cloudflare Pages project configuration
# KV namespace binding for newsletter subscriptions
# Deployed via: git push → Cloudflare Pages auto-build

name = "china-travel-kit"
compatibility_date = "2024-01-01"

# KV namespaces — bindings must also be set in Cloudflare Dashboard:
# Pages → china-travel-kit → Settings → Functions → KV namespace bindings
[[kv_namespaces]]
binding = "NEWSLETTER"
id = "4b6726cf8a114b9d8e9071d85521434f"
