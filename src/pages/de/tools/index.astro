---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import Header from '../../../components/Header.astro';
import Footer from '../../../components/Footer.astro';
import AdSlot from '../../../components/AdSlot.astro';
import { TOOLS as tools } from '../../../config';

const title = 'Kostenlose Tools für China-Reisen: Visum, Bezahlung, eSIM, Budget und mehr';
const description =
  'Kostenlose interaktive Tools für die Reiseplanung in China: visumfreie Einreise prüfen, Karten-Kompatibilität einschätzen, eSIMs vergleichen, Budget kalkulieren, Netzstecker checken und Adresskarten oder wichtige Sätze vorbereiten.';

const toolUseCases = [
  {
    title: 'Vor der Buchung',
    text: 'Prüfe zuerst Einreise, Reisezeit und grobes Budget, bevor du Reisedaten festlegst oder Städte kombinierst.',
    links: [
      { href: '/de/tools/visa-free-checker/', label: 'Visumfrei-Prüfung' },
      { href: '/de/tools/best-time-to-visit/', label: 'Beste Reisezeit' },
      { href: '/de/tools/budget-cash-estimator/', label: 'Budgetrechner' },
    ],
  },
  {
    title: 'Vor dem Abflug',
    text: 'Nutze die Tools zu Bezahlung, eSIM, App-Zugang, Steckern und Redewendungen, solange du zu Hause noch alles in Ruhe einrichten kannst.',
    links: [
      { href: '/de/tools/payment-checker/', label: 'Zahlungsprüfung' },
      { href: '/de/tools/esim-comparison/', label: 'eSIM-Vergleich' },
      { href: '/de/tools/app-availability-checker/', label: 'App-Prüfung' },
    ],
  },
  {
    title: 'Vor Ort in China',
    text: 'Wenn etwas hakt, helfen Fahrerkarten, Währungsumrechnung, Überlebenskit und wichtige Sätze bei Taxi, Hotel, Restaurant oder Bahnhof weiter.',
    links: [
      { href: '/de/tools/survival-kit/', label: 'Überlebenskit' },
      { href: '/de/tools/show-to-driver/', label: 'Dem Fahrer zeigen' },
      { href: '/de/tools/currency-converter/', label: 'RMB-Umrechner' },
      { href: '/de/tools/essential-phrases/', label: 'Wichtige Sätze' },
    ],
  },
];

const toolsJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'CollectionPage',
  name: title,
  description,
  hasPart: tools.map((tool) => ({
    '@type': 'WebApplication',
    name: tool.title,
    description: tool.desc,
    applicationCategory: 'TravelApplication',
    url: new URL(tool.href, 'https://www.chinatripbox.com').toString(),
  })),
};
---

<BaseLayout title={`${title} | ChinaTripBox`} description={description} locale="de">
  <script slot="head" type="application/ld+json" set:html={JSON.stringify(toolsJsonLd)} />
  <Header locale="de" />
  <main class="container-page py-12">
    <header class="max-w-2xl">
      <p class="section-kicker">Tool-Hub</p>
      <h1 class="mt-2 text-3xl font-bold text-gray-900 md:text-4xl">Kostenlose China-Reisetools</h1>
      <p class="mt-3 text-lg text-gray-600">{description}</p>
      <div class="mt-5 flex flex-wrap gap-3">
        <a href="/de/posts/what-to-set-up-before-traveling-to-china-alipay-esim-didi-train-tickets/" class="btn-primary">Zur Checkliste vor der Reise</a>
        <a href="/de/posts/china-trip-planner-first-time-visitors-payment-esim-transport-apps/" class="btn-outline">Planer für Erstbesucher lesen</a>
      </div>
    </header>

    <AdSlot format="banner" />

    <section class="mt-8 grid gap-5 lg:grid-cols-3" aria-labelledby="tool-use-cases-heading">
      <div class="lg:col-span-3">
        <h2 id="tool-use-cases-heading" class="text-2xl font-bold text-gray-950">Welches Tool ist der beste Einstieg?</h2>
      </div>
      {toolUseCases.map((useCase) => (
        <article class="rounded-lg border border-gray-200 bg-white p-5 shadow-sm">
          <h3 class="text-lg font-bold text-gray-950">{useCase.title}</h3>
          <p class="mt-2 text-sm leading-6 text-gray-600">{useCase.text}</p>
          <div class="mt-4 flex flex-wrap gap-2">
            {useCase.links.map((link) => (
              <a href={link.href} class="rounded-full border border-gray-200 px-3 py-1.5 text-xs font-semibold text-brand-700 hover:border-brand-300 hover:bg-brand-50 hover:no-underline">{link.label}</a>
            ))}
          </div>
        </article>
      ))}
    </section>

    <section class="mt-8 grid gap-6 md:grid-cols-2">
      {tools.map((tool) => (
        <a href={tool.href} class="card block hover:no-underline">
          <div class="text-3xl">{tool.icon}</div>
          <h2 class="mt-3 text-lg font-bold text-gray-900">{tool.title}</h2>
          <p class="mt-1.5 text-sm text-gray-600">{tool.desc}</p>
          <span class="mt-3 inline-block text-sm font-semibold text-brand-600">Jetzt öffnen →</span>
        </a>
      ))}
    </section>
  </main>
  <Footer />
</BaseLayout>
