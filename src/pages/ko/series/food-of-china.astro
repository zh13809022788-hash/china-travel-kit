---
import { getCollection } from 'astro:content';
import BaseLayout from '../../../layouts/BaseLayout.astro';
import Header from '../../../components/Header.astro';
import Footer from '../../../components/Footer.astro';
import AdSlot from '../../../components/AdSlot.astro';

const seriesSlug = 'food-of-china';
const posts = (await getCollection('posts-ko'))
  .filter((p) => p.data.series === seriesSlug)
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

const title = '중국 음식 - 외국인의 미식 여행';
const description = '외국인 여행자의 시선으로 중국 음식 문화를 살펴보세요. 8대 요리, 지역 훠궈, 길거리 음식 안전, 딤섬, 아침 식사, 채식 옵션, 그리고 도시를 넘어 찾아갈 만한 요리까지 담았습니다.';

const relatedTools = [
  { href: '/ko/posts/ordering-food-in-china-without-chinese-meituan-ele-me/', title: '중국어 없이 음식 주문하기', desc: '외국인을 위한 Meituan & Ele.me 설정' },
  { href: '/ko/posts/how-to-pay-in-china-tourist-guide/', title: '중국에서 결제하는 방법', desc: '식사할 때의 Alipay, WeChat Pay, 현금 사용법' },
  { href: '/ko/food/', title: '음식과 식사 가이드', desc: 'ChinaTripBox의 모든 음식 관련 글' },
];

const relatedCities = [
  { href: '/ko/posts/chengdu-city-guide-for-foreigners/', title: '청두', desc: '사천 요리의 중심지' },
  { href: '/ko/posts/beijing-city-guide-for-foreigners/', title: '베이징', desc: '북경오리와 북방 요리' },
  { href: '/ko/posts/shanghai-city-guide-for-foreigners/', title: '상하이', desc: '샤오롱바오와 퓨전 요리' },
];

const otherSeries = [
  { href: '/ko/series/history-of-china/', title: '중국 역사' },
  { href: '/ko/series/culture-of-china/', title: '중국 문화' },
  { href: '/ko/series/modern-china/', title: '현대 중국' },
];
import { getCategoryLabel } from '../../../i18n/categories';
const locale = 'ko';
---

<BaseLayout title={`${title} | ChinaTripBox`} description={description} locale="ko">
  <Header locale="ko" />
  <main class="container-page py-10">
    <header class="max-w-3xl">
      <p class="section-kicker">콘텐츠 시리즈</p>
      <h1 class="mt-2 text-4xl font-bold leading-tight text-gray-950">{title}</h1>
      <p class="mt-4 text-lg leading-8 text-gray-600">{description}</p>
    </header>

    <AdSlot format="banner" />

    <section class="mt-10 grid gap-8 lg:grid-cols-[1fr_320px]">
      <div>
        <div class="section-heading">
          <div>
            <p class="section-kicker">아티클</p>
            <h2>중국 음식 시리즈</h2>
          </div>
        </div>
        {posts.length > 0 ? (
          <div class="mt-6 grid gap-5 md:grid-cols-2">
            {posts.map((post) => (
              <a href={`/ko/posts/${post.slug}/`} class="card block hover:no-underline">
                <span class="text-xs font-semibold uppercase tracking-wide text-brand-700">{getCategoryLabel(post.data.category, locale)}</span>
                <h3 class="mt-2 text-lg font-bold text-gray-950">{post.data.title}</h3>
                <p class="mt-2 text-sm leading-6 text-gray-600">{post.data.description}</p>
              </a>
            ))}
          </div>
        ) : (
          <p class="mt-6 text-gray-500">이 시리즈의 글은 준비 중입니다. 곧 다시 확인해 주세요.</p>
        )}
      </div>

      <aside class="space-y-6">
        <div class="rounded-lg border border-gray-200 bg-white p-5 shadow-sm">
          <p class="section-kicker">실용 도구</p>
          <h2 class="mt-1 text-xl font-bold text-gray-950">중국에서 맛있게 먹기</h2>
          <div class="mt-4 grid gap-3">
            {relatedTools.map((item) => (
              <a href={item.href} class="rounded-lg border border-gray-200 p-4 hover:border-brand-200 hover:bg-brand-50 hover:no-underline">
                <h3 class="text-sm font-semibold text-gray-950">{item.title}</h3>
                <p class="mt-1 text-sm leading-6 text-gray-600">{item.desc}</p>
              </a>
            ))}
          </div>
        </div>

        <div class="rounded-lg border border-gray-200 bg-white p-5 shadow-sm">
          <p class="section-kicker">도시별 음식 가이드</p>
          <h2 class="mt-1 text-xl font-bold text-gray-950">어디서 먹을까</h2>
          <div class="mt-4 grid gap-3">
            {relatedCities.map((item) => (
              <a href={item.href} class="rounded-lg border border-gray-200 p-4 hover:border-brand-200 hover:bg-brand-50 hover:no-underline">
                <h3 class="text-sm font-semibold text-gray-950">{item.title}</h3>
                <p class="mt-1 text-sm leading-6 text-gray-600">{item.desc}</p>
              </a>
            ))}
          </div>
        </div>

        <div class="rounded-lg border border-gray-200 bg-white p-5 shadow-sm">
          <p class="section-kicker">다른 시리즈</p>
          <div class="mt-3 grid gap-2">
            {otherSeries.map((s) => (
              <a href={s.href} class="text-sm font-semibold text-brand-700 hover:underline">{s.title}</a>
            ))}
          </div>
        </div>
      </aside>
    </section>
  </main>
  <Footer />
</BaseLayout>