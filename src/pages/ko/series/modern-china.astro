---
import { getCollection } from 'astro:content';
import BaseLayout from '../../../layouts/BaseLayout.astro';
import Header from '../../../components/Header.astro';
import Footer from '../../../components/Footer.astro';
import AdSlot from '../../../components/AdSlot.astro';

const seriesSlug = 'modern-china';
const posts = (await getCollection('posts-ko'))
  .filter((p) => p.data.series === seriesSlug)
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

const title = '현대 중국, 외국인이 가장 놀라는 것들';
const description = '외국인 여행자의 시선으로 현대 중국을 만나보세요. 현금 없는 사회, 세계에서 가장 빠른 열차, 초대형 도시 이동, 슈퍼 앱, 야간열차, 그리고 일상을 규정하는 기술까지 살펴봅니다.';

const relatedTools = [
  { href: '/ko/posts/how-to-pay-in-china-tourist-guide/', title: '중국에서 결제하는 방법', desc: '관광객을 위한 모바일 결제 설정' },
  { href: '/ko/posts/china-high-speed-rail-guide-foreigners/', title: '고속철도 가이드', desc: '중국의 초고속 열차 타는 법' },
  { href: '/ko/posts/best-esim-for-china-travel-2026/', title: '중국 여행용 최고의 eSIM', desc: '도착하자마자 바로 연결하세요' },
];

const relatedCities = [
  { href: '/ko/posts/shanghai-city-guide-for-foreigners/', title: '상하이', desc: '중국의 금융 중심 메가시티' },
  { href: '/ko/posts/beijing-city-guide-for-foreigners/', title: '베이징', desc: '고대 수도와 현대 허브가 만나는 도시' },
  { href: '/ko/posts/shanghai-metro-for-foreigners-tickets-qr-codes-transfers/', title: '상하이 지하철', desc: '초대형 대중교통 시스템 이용하기' },
];

const otherSeries = [
  { href: '/ko/series/food-of-china/', title: '중국의 음식' },
  { href: '/ko/series/history-of-china/', title: '중국의 역사' },
  { href: '/ko/series/culture-of-china/', title: '중국의 문화' },
];
import { getCategoryLabel } from '../../../i18n/categories';
const locale = 'ko';
---

<BaseLayout title={`${title} | ChinaTripBox`} description={description} locale="ko">
  <Header locale="ko" />
  <main class="container-page py-10">
    <header class="max-w-3xl">
      <p class="section-kicker">콘텐츠 시리즈</p>
      <h1 class="mt-2 text-4xl font-bold leading-tight text-gray-950">{title}</h1>
      <p class="mt-4 text-lg leading-8 text-gray-600">{description}</p>
    </header>

    <AdSlot format="banner" />

    <section class="mt-10 grid gap-8 lg:grid-cols-[1fr_320px]">
      <div>
        <div class="section-heading">
          <div>
            <p class="section-kicker">글 목록</p>
            <h2>현대 중국 시리즈</h2>
          </div>
        </div>
        {posts.length > 0 ? (
          <div class="mt-6 grid gap-5 md:grid-cols-2">
            {posts.map((post) => (
              <a href={`/ko/posts/${post.slug}/`} class="card block hover:no-underline">
                <span class="text-xs font-semibold uppercase tracking-wide text-brand-700">{getCategoryLabel(post.data.category, locale)}</span>
                <h3 class="mt-2 text-lg font-bold text-gray-950">{post.data.title}</h3>
                <p class="mt-2 text-sm leading-6 text-gray-600">{post.data.description}</p>
              </a>
            ))}
          </div>
        ) : (
          <p class="mt-6 text-gray-500">이 시리즈의 글은 현재 준비 중입니다. 곧 다시 확인해 주세요.</p>
        )}
      </div>

      <aside class="space-y-6">
        <div class="rounded-lg border border-gray-200 bg-white p-5 shadow-sm">
          <p class="section-kicker">실용 가이드</p>
          <h2 class="mt-1 text-xl font-bold text-gray-950">현대 중국을 더 쉽게 이용하기</h2>
          <div class="mt-4 grid gap-3">
            {relatedTools.map((item) => (
              <a href={item.href} class="rounded-lg border border-gray-200 p-4 hover:border-brand-200 hover:bg-brand-50 hover:no-underline">
                <h3 class="text-sm font-semibold text-gray-950">{item.title}</h3>
                <p class="mt-1 text-sm leading-6 text-gray-600">{item.desc}</p>
              </a>
            ))}
          </div>
        </div>

        <div class="rounded-lg border border-gray-200 bg-white p-5 shadow-sm">
          <p class="section-kicker">도시 가이드</p>
          <h2 class="mt-1 text-xl font-bold text-gray-950">메가시티</h2>
          <div class="mt-4 grid gap-3">
            {relatedCities.map((item) => (
              <a href={item.href} class="rounded-lg border border-gray-200 p-4 hover:border-brand-200 hover:bg-brand-50 hover:no-underline">
                <h3 class="text-sm font-semibold text-gray-950">{item.title}</h3>
                <p class="mt-1 text-sm leading-6 text-gray-600">{item.desc}</p>
              </a>
            ))}
          </div>
        </div>

        <div class="rounded-lg border border-gray-200 bg-white p-5 shadow-sm">
          <p class="section-kicker">다른 시리즈</p>
          <div class="mt-3 grid gap-2">
            {otherSeries.map((s) => (
              <a href={s.href} class="text-sm font-semibold text-brand-700 hover:underline">{s.title}</a>
            ))}
          </div>
        </div>
      </aside>
    </section>
  </main>
  <Footer />
</BaseLayout>