---
import { getCollection } from 'astro:content';
import BaseLayout from '../../../layouts/BaseLayout.astro';
import Header from '../../../components/Header.astro';
import Footer from '../../../components/Footer.astro';
import AdSlot from '../../../components/AdSlot.astro';
import CoverImage from '../../../components/CoverImage.astro';

const CATEGORY = 'transport';
const posts = (await getCollection('posts-fr'))
  .filter((p) => p.data.category === CATEGORY)
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

const title = 'Transport en Chine : trains, VTC et déplacements';
const description =
  'Apprenez à réserver les trains à grande vitesse, utiliser DiDi et vous déplacer en transports publics en Chine.';

const transportPaths = [
  { href: '/fr/posts/china-high-speed-rail-guide-foreigners/', title: 'Trains longue distance', desc: 'Utilisez ce guide pour planifier vos trajets entre villes, choisir votre classe, gérer l’entrée avec passeport et le timing en gare.' },
  { href: '/fr/posts/airport-to-city-transport-beijing-shanghai-guangzhou/', title: 'Arrivée depuis l’aéroport', desc: 'Comparez le métro, le taxi, les VTC et le rail aéroport pour votre premier trajet en ville.' },
  { href: '/fr/posts/didi-china-foreign-tourists-guide/', title: 'VTC', desc: 'Configurez DiDi, utilisez des points de prise en charge traduits et réduisez les frictions avec le chauffeur.' },
  { href: '/fr/posts/shanghai-metro-for-foreigners-tickets-qr-codes-transfers/', title: 'Réseaux de métro', desc: 'Découvrez les billets QR, les correspondances et les habitudes aux heures de pointe à travers l’exemple de Shanghai.' },
];
import { getCategoryLabel } from '../../../i18n/categories';
const locale = 'fr';
---

<BaseLayout title={`${title} | ChinaTripBox`} description={description} locale="fr">
  <Header locale="fr" />
  <main class="container-page py-10">
    <header class="max-w-2xl">
      <span class="text-sm font-semibold uppercase tracking-wide text-brand-600">Transport</span>
      <h1 class="mt-2 text-3xl font-bold text-gray-900 md:text-4xl">Guides de transport en Chine</h1>
      <p class="mt-3 text-lg text-gray-600">{description}</p>
    </header>

    <CoverImage name="hub-transport" alt="Train moderne à grande vitesse et réseau ferroviaire en Chine" loading="lazy" />

    <AdSlot format="banner" />

    <section class="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4" aria-labelledby="featured-transport">
      <h2 id="featured-transport" class="sr-only">Guides de transport à lire</h2>
      <a href="/fr/posts/china-high-speed-rail-guide-foreigners/" class="rounded-lg border border-gray-200 bg-white p-4 text-sm shadow-sm transition-colors hover:border-brand-300 hover:bg-brand-50 hover:no-underline">
        <span class="font-semibold text-gray-950">Guide du train à grande vitesse</span>
        <p class="mt-1 text-xs leading-5 text-gray-600">Réserver des billets, choisir une classe, se repérer en gare</p>
      </a>
      <a href="/fr/posts/book-china-train-tickets-12306-foreigners/" class="rounded-lg border border-gray-200 bg-white p-4 text-sm shadow-sm transition-colors hover:border-brand-300 hover:bg-brand-50 hover:no-underline">
        <span class="font-semibold text-gray-950">Réservation 12306</span>
        <p class="mt-1 text-xs leading-5 text-gray-600">Guide pas à pas avec passeport étranger</p>
      </a>
      <a href="/fr/posts/didi-china-foreign-tourists-guide/" class="rounded-lg border border-gray-200 bg-white p-4 text-sm shadow-sm transition-colors hover:border-brand-300 hover:bg-brand-50 hover:no-underline">
        <span class="font-semibold text-gray-950">VTC DiDi</span>
        <p class="mt-1 text-xs leading-5 text-gray-600">Comment utiliser l’application de VTC en Chine</p>
      </a>
      <a href="/fr/posts/airport-to-city-transport-beijing-shanghai-guangzhou/" class="rounded-lg border border-gray-200 bg-white p-4 text-sm shadow-sm transition-colors hover:border-brand-300 hover:bg-brand-50 hover:no-underline">
        <span class="font-semibold text-gray-950">Transferts aéroport</span>
        <p class="mt-1 text-xs leading-5 text-gray-600">Guides pour Pékin, Shanghai et Guangzhou</p>
      </a>
    </section>

    <section class="mt-8 grid gap-4 md:grid-cols-2 xl:grid-cols-4" aria-labelledby="transport-paths">
      <h2 id="transport-paths" class="sr-only">Parcours de transport</h2>
      {transportPaths.map((path) => (
        <a href={path.href} class="card block hover:no-underline">
          <h3 class="text-lg font-bold text-gray-950">{path.title}</h3>
          <p class="mt-2 text-sm leading-6 text-gray-600">{path.desc}</p>
        </a>
      ))}
    </section>

    <section class="mt-8 grid gap-6 md:grid-cols-2">
      {posts.map((post, i) => (
        <>
          <a href={`/fr/posts/${post.slug}/`} class="card block hover:no-underline">
            <span class="text-xs font-semibold uppercase tracking-wide text-brand-600">{getCategoryLabel(post.data.category, locale)}</span>
            <h2 class="mt-1.5 text-lg font-bold text-gray-900">{post.data.title}</h2>
            <p class="mt-1.5 text-sm text-gray-600">{post.data.description}</p>
            <time datetime={post.data.pubDate.toISOString()} class="mt-3 block text-xs text-gray-400">
              {post.data.pubDate.toLocaleDateString('fr-FR', { year: 'numeric', month: 'short', day: 'numeric' })}
            </time>
          </a>
          {(i + 1) % 3 === 0 && <div class="md:col-span-2"><AdSlot format="in-article" /></div>}
        </>
      ))}
      {posts.length === 0 && <p class="text-gray-500">Guides bientôt disponibles.</p>}
    </section>
  </main>
  <Footer />
</BaseLayout>

---