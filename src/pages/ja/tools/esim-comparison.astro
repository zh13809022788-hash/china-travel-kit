---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import Header from '../../../components/Header.astro';
import Footer from '../../../components/Footer.astro';
import AdSlot from '../../../components/AdSlot.astro';
import AffiliateCta from '../../../components/AffiliateCta.astro';
import { AFFILIATE_LINKS } from '../../../config';

const title = 'China旅行向けeSIMプラン比較';
const description =
  'China旅行向けの主要eSIMプランを、データ容量、有効期間、価格、ネットワークで比較できます。フィルターと並べ替えで、旅程に最適なプランを見つけましょう。';

// China旅行向けeSIM商品のプリセット8件（代表的なサンプルデータ）
const plans = [
  { id: 1, name: 'China エクスプローラー 5GB', data: 5, dataLabel: '5 GB', days: 7, price: 9.5, network: 'China Unicom', note: '現地アプリの制限を回避' },
  { id: 2, name: 'China エクスプローラー 10GB', data: 10, dataLabel: '10 GB', days: 15, price: 16, network: 'China Unicom', note: '現地アプリの制限を回避' },
  { id: 3, name: '本土ライト 3GB', data: 3, dataLabel: '3 GB', days: 7, price: 6.5, network: 'China Mobile', note: '現地ネットワーク、VPNルーティングなし' },
  { id: 4, name: 'グレーターChina 8GB', data: 8, dataLabel: '8 GB', days: 30, price: 18.9, network: 'China Unicom', note: 'CN、HK、MO、TWに対応' },
  { id: 5, name: 'デイリー無制限', data: 9999, dataLabel: '無制限*', days: 10, price: 29, network: 'China Unicom', note: '1日2GB超過後はフェアユース上限あり' },
  { id: 6, name: 'ビジネス 20GB', data: 20, dataLabel: '20 GB', days: 30, price: 32, network: 'China Unicom', note: '現地アプリの制限を回避' },
  { id: 7, name: '短期旅行 1GB', data: 1, dataLabel: '1 GB', days: 3, price: 3.2, network: 'China Mobile', note: '短時間の乗り継ぎに最適' },
  { id: 8, name: 'ファミリー 30GB', data: 30, dataLabel: '30 GB', days: 30, price: 45, network: 'China Unicom', note: 'ホットスポットで共有可能' },
];
---

<BaseLayout title={`${title} | ChinaTripBox`} description={description} locale="ja">
  <Header locale="ja" />
  <main class="container-page py-10">
    <header class="max-w-2xl">
      <span class="text-sm font-semibold uppercase tracking-wide text-brand-600">ツール</span>
      <h1 class="mt-2 text-3xl font-bold text-gray-900 md:text-4xl">eSIMプラン比較</h1>
      <p class="mt-3 text-lg text-gray-600">{description}</p>
    </header>

    <AdSlot format="banner" />

    <!-- コントロール -->
    <div class="mt-6 flex flex-wrap items-end gap-4 rounded-xl border border-gray-200 bg-gray-50 p-4">
      <label class="flex flex-col text-sm">
        <span class="mb-1 font-medium text-gray-700">並べ替え</span>
        <select id="sort-by" class="rounded-lg border border-gray-300 px-3 py-2 text-sm">
          <option value="price-asc">価格: 安い順</option>
          <option value="price-desc">価格: 高い順</option>
          <option value="data-desc">データ容量: 多い順</option>
          <option value="days-desc">有効期間: 長い順</option>
        </select>
      </label>
      <label class="flex flex-col text-sm">
        <span class="mb-1 font-medium text-gray-700">ネットワーク</span>
        <select id="filter-network" class="rounded-lg border border-gray-300 px-3 py-2 text-sm">
          <option value="all">すべてのネットワーク</option>
          <option value="China Unicom">China Unicom</option>
          <option value="China Mobile">China Mobile</option>
        </select>
      </label>
      <label class="flex flex-col text-sm">
        <span class="mb-1 font-medium text-gray-700">最小データ容量 (GB)</span>
        <select id="filter-data" class="rounded-lg border border-gray-300 px-3 py-2 text-sm">
          <option value="0">指定なし</option>
          <option value="5">5+ GB</option>
          <option value="10">10+ GB</option>
          <option value="20">20+ GB</option>
        </select>
      </label>
    </div>

    <!-- 比較表 -->
    <div class="mt-6 overflow-x-auto">
      <table class="w-full min-w-[720px] border-collapse text-sm">
        <thead>
          <tr class="border-b-2 border-gray-200 text-left text-gray-500">
            <th class="py-3 pr-4 font-semibold">プラン</th>
            <th class="py-3 pr-4 font-semibold">データ容量</th>
            <th class="py-3 pr-4 font-semibold">有効期間</th>
            <th class="py-3 pr-4 font-semibold">価格 (USD)</th>
            <th class="py-3 pr-4 font-semibold">ネットワーク</th>
            <th class="py-3 pr-4 font-semibold">備考</th>
          </tr>
        </thead>
        <tbody id="plan-rows"></tbody>
      </table>
      <p id="no-results" class="hidden py-8 text-center text-gray-500">条件に一致するプランはありません。</p>
    </div>

    <AffiliateCta
      title="すぐに接続する準備はできましたか？"
      description="おすすめのChina eSIMを有効化して、到着した瞬間からオンラインになりましょう。"
      buttonText="eSIMを入手"
      placeholder={AFFILIATE_LINKS.esim}
    />
  </main>
  <Footer />
</BaseLayout>

<script define:vars={{ plans }}>
  const rowsEl = document.getElementById('plan-rows');
  const noResults = document.getElementById('no-results');
  const sortBy = document.getElementById('sort-by');
  const filterNetwork = document.getElementById('filter-network');
  const filterData = document.getElementById('filter-data');

  function render() {
    let list = [...plans];
    const net = filterNetwork.value;
    const minData = Number(filterData.value);

    if (net !== 'all') list = list.filter((p) => p.network === net);
    if (minData > 0) list = list.filter((p) => p.data >= minData);

    switch (sortBy.value) {
      case 'price-asc': list.sort((a, b) => a.price - b.price); break;
      case 'price-desc': list.sort((a, b) => b.price - a.price); break;
      case 'data-desc': list.sort((a, b) => b.data - a.data); break;
      case 'days-desc': list.sort((a, b) => b.days - a.days); break;
    }

    rowsEl.innerHTML = list
      .map(
        (p) => `
      <tr class="border-b border-gray-100">
        <td class="py-3 pr-4 font-semibold text-gray-900">${p.name}</td>
        <td class="py-3 pr-4">${p.dataLabel}</td>
        <td class="py-3 pr-4">${p.days}日間</td>
        <td class="py-3 pr-4 font-semibold">$${p.price.toFixed(2)}</td>
        <td class="py-3 pr-4">${p.network}</td>
        <td class="py-3 pr-4 text-gray-500">${p.note}</td>
      </tr>`
      )
      .join('');

    noResults.classList.toggle('hidden', list.length > 0);
  }

  [sortBy, filterNetwork, filterData].forEach((el) => el.addEventListener('change', render));
  render();
</script>
