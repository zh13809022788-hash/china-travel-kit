---
import { getCollection } from 'astro:content';
import BaseLayout from '../../../../layouts/BaseLayout.astro';
import Header from '../../../../components/Header.astro';
import Footer from '../../../../components/Footer.astro';
import AdSlot from '../../../../components/AdSlot.astro';

const seriesSlug = 'modern-china';
const posts = (await getCollection('posts-zh-tw'))
  .filter((p) => p.data.series === seriesSlug)
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

const title = '現代中國：最讓外國人驚訝的地方';
const description = '以外國旅客的視角探索現代中國：無現金社會、全球最快的列車、超大城市導航、超級應用程式、夜間列車，以及定義日常生活的科技。';

const relatedTools = [
  { href: '/zh-tw/posts/how-to-pay-in-china-tourist-guide/', title: '在中國如何付款', desc: '旅客行動支付設定' },
  { href: '/zh-tw/posts/china-high-speed-rail-guide-foreigners/', title: '高鐵指南', desc: '搭乘中國的高速列車' },
  { href: '/zh-tw/posts/best-esim-for-china-travel-2026/', title: '中國最佳 eSIM', desc: '抵達後立即保持連線' },
];

const relatedCities = [
  { href: '/zh-tw/posts/shanghai-city-guide-for-foreigners/', title: '上海', desc: '中國的金融超大城市' },
  { href: '/zh-tw/posts/beijing-city-guide-for-foreigners/', title: '北京', desc: '古都與現代樞紐交會' },
  { href: '/zh-tw/posts/shanghai-metro-for-foreigners-tickets-qr-codes-transfers/', title: '上海地鐵', desc: '掌握龐大的大眾運輸系統' },
];

const otherSeries = [
  { href: '/zh-tw/series/food-of-china/', title: '中國美食' },
  { href: '/zh-tw/series/history-of-china/', title: '中國歷史' },
  { href: '/zh-tw/series/culture-of-china/', title: '中國文化' },
];
import { getCategoryLabel } from '../../../../i18n/categories';
const locale = 'zh-tw';
---

<BaseLayout title={`${title} | ChinaTripBox`} description={description} locale="zh-TW">
  <Header locale="zh-TW" />
  <main class="container-page py-10">
    <header class="max-w-3xl">
      <p class="section-kicker">內容系列</p>
      <h1 class="mt-2 text-4xl font-bold leading-tight text-gray-950">{title}</h1>
      <p class="mt-4 text-lg leading-8 text-gray-600">{description}</p>
    </header>

    <AdSlot format="banner" />

    <section class="mt-10 grid gap-8 lg:grid-cols-[1fr_320px]">
      <div>
        <div class="section-heading">
          <div>
            <p class="section-kicker">文章</p>
            <h2>現代中國系列</h2>
          </div>
        </div>
        {posts.length > 0 ? (
          <div class="mt-6 grid gap-5 md:grid-cols-2">
            {posts.map((post) => (
              <a href={`/zh-tw/posts/${post.slug}/`} class="card block hover:no-underline">
                <span class="text-xs font-semibold uppercase tracking-wide text-brand-700">{getCategoryLabel(post.data.category, locale)}</span>
                <h3 class="mt-2 text-lg font-bold text-gray-950">{post.data.title}</h3>
                <p class="mt-2 text-sm leading-6 text-gray-600">{post.data.description}</p>
              </a>
            ))}
          </div>
        ) : (
          <p class="mt-6 text-gray-500">本系列文章正在準備中，請稍後再回來查看。</p>
        )}
      </div>

      <aside class="space-y-6">
        <div class="rounded-lg border border-gray-200 bg-white p-5 shadow-sm">
          <p class="section-kicker">實用工具</p>
          <h2 class="mt-1 text-xl font-bold text-gray-950">暢行現代中國</h2>
          <div class="mt-4 grid gap-3">
            {relatedTools.map((item) => (
              <a href={item.href} class="rounded-lg border border-gray-200 p-4 hover:border-brand-200 hover:bg-brand-50 hover:no-underline">
                <h3 class="text-sm font-semibold text-gray-950">{item.title}</h3>
                <p class="mt-1 text-sm leading-6 text-gray-600">{item.desc}</p>
              </a>
            ))}
          </div>
        </div>

        <div class="rounded-lg border border-gray-200 bg-white p-5 shadow-sm">
          <p class="section-kicker">城市指南</p>
          <h2 class="mt-1 text-xl font-bold text-gray-950">超大城市</h2>
          <div class="mt-4 grid gap-3">
            {relatedCities.map((item) => (
              <a href={item.href} class="rounded-lg border border-gray-200 p-4 hover:border-brand-200 hover:bg-brand-50 hover:no-underline">
                <h3 class="text-sm font-semibold text-gray-950">{item.title}</h3>
                <p class="mt-1 text-sm leading-6 text-gray-600">{item.desc}</p>
              </a>
            ))}
          </div>
        </div>

        <div class="rounded-lg border border-gray-200 bg-white p-5 shadow-sm">
          <p class="section-kicker">更多系列</p>
          <div class="mt-3 grid gap-2">
            {otherSeries.map((s) => (
              <a href={s.href} class="text-sm font-semibold text-brand-700 hover:underline">{s.title}</a>
            ))}
          </div>
        </div>
      </aside>
    </section>
  </main>
  <Footer />
</BaseLayout>